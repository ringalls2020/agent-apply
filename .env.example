# Zero-config local default (works without PostgreSQL):
DATABASE_URL=sqlite+pysqlite:///./agent_apply.db

# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/agent_apply
